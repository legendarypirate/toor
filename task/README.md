# Task API (Express + Sequelize)

Backend REST API for the Outdoor ecommerce monorepo.

## Setup

```bash
npm install
```

## Run

```bash
node server.js
```

Configure database and environment variables as required for your environment (see project root / deployment docs).
